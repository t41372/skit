# Babel string-extraction config (dev-only; see scripts/i18n.py).
# Patterns are matched relative to the extraction root (src/skit), so this is just **.py.
# gettext(), ngettext(), and N_() are Babel default keywords, so no [keywords] section is needed.
[python: **.py]
