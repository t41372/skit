name: Config oracle probe

on:
  pull_request:
    paths:
      - .github/workflows/config-oracle-probe.yml
      - crates/skit-store/tests/port_test_config.rs

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Run config oracle
        shell: bash
        run: |
          set +e
          cargo test --locked -p skit-store --test port_test_config -- --nocapture
          rc=$?
          echo "CONFIG_ORACLE_RC=$rc"
          exit 0
