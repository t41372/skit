name: Default state oracle probe

on:
  pull_request:
    paths:
      - .github/workflows/default-state-oracle-probe.yml
      - crates/skit-store/tests/port_test_default_state_semantics.rs

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Run default state oracle
        shell: bash
        run: |
          set +e
          cargo test --locked -p skit-store --test port_test_default_state_semantics -- --nocapture
          rc=$?
          echo "DEFAULT_STATE_ORACLE_RC=$rc"
          exit 0
