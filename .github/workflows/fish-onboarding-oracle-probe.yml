name: Fish onboarding oracle probe

on:
  pull_request:
    paths:
      - .github/workflows/fish-onboarding-oracle-probe.yml
      - crates/skit-form/tests/port_test_onboarding_fish.rs

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Run fish onboarding oracle
        shell: bash
        run: |
          set +e
          cargo test --locked -p skit-form --test port_test_onboarding_fish -- --nocapture
          rc=$?
          echo "FISH_ONBOARDING_ORACLE_RC=$rc"
          exit 0
