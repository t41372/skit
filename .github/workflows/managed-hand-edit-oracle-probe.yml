name: Managed hand edit oracle probe

on:
  pull_request:
    paths:
      - .github/workflows/managed-hand-edit-oracle-probe.yml
      - crates/skit-language/tests/port_test_managed_metadata_hand_edit.rs

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Run hand-edited metadata oracle
        shell: bash
        run: |
          set +e
          cargo test --locked -p skit-language --test port_test_managed_metadata_hand_edit -- --nocapture
          rc=$?
          echo "MANAGED_HAND_EDIT_ORACLE_RC=$rc"
          exit 0
