# The authoritative gate merges LCOV records and checks every executable source line locally.
# Codecov is only a dashboard. An external service cannot define correctness.
coverage:
  status:
    project:
      default:
        target: 100%
        informational: true
    patch:
      default:
        informational: true
