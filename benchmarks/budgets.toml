# The performance contract (docs/design/benchmarks.md — "Budgets").
#
# tier = "enforced": `benchmarks check` fails on violation. Deterministic or
#   ratchet-safe metrics only; every row carries context = provenance for its bound.
# tier = "target": the aspirational contract — reported on every run, never failing
#   CI until a future PR deliberately moves the row to enforced.
# ratchet = true: the bound derives from a measured value + headroom, and
#   `check --propose` refreshes it (from the CI artifact — never a local run; the
#   module census is python-version-dependent). Non-ratchet bounds are hand-set
#   contract values that propose leaves alone.
#
# This file is regenerated by `uv run python -m benchmarks check --propose`; keep
# prose in `note` fields, not comments below this header.

[[budget]]
metric = "footprint.wheel_bytes"
max = 1048576
tier = "enforced"
profiles = [
    "pr",
    "full",
]
note = "1 MiB hand-set contract bound (wheel is ~451 KiB); catches accidental data shipping"

[budget.context]
commit = "366d6d4"
date = "2026-07-20"

[[budget]]
metric = "imports.version.modules"
max = 400
tier = "enforced"
ratchet = true
headroom = 0.1
note = "ratchet: the fast path may not get importier before the fast-path PR lands (provisional bootstrap bound; tighten from the CI artifact via check --propose)"

[budget.context]
python = "3.13"
commit = "366d6d4"
date = "2026-07-20"

[[budget]]
metric = "imports.list_json.modules"
max = 400
tier = "enforced"
ratchet = true
headroom = 0.1
note = "ratchet: list's import graph may not grow before the fast-path PR lands (provisional bootstrap bound; tighten from the CI artifact via check --propose)"

[budget.context]
python = "3.13"
commit = "366d6d4"
date = "2026-07-20"

[[budget]]
metric = "pipeline.skipped_count"
max = 0
tier = "enforced"
profiles = [
    "pr",
]
platform = "linux-x86_64"
ci_only = true
note = "the pr profile may not silently decay on the reference CI platform"

[budget.context]
commit = "366d6d4"
date = "2026-07-20"

[[budget]]
metric = "pipeline.skipped_count"
max = 0
tier = "enforced"
profiles = [
    "full",
]
platform = "linux-x86_64"
ci_only = true
note = "the skip-prone suites (JS lane, syscalls, closure) run only nightly; the full profile may not silently decay either"

[budget.context]
commit = "366d6d4"
date = "2026-07-20"

[[budget]]
metric = "startup.version.over_python_ms"
max = 75
tier = "target"
note = "skit --version at most 75 ms over a bare python -c pass"

[[budget]]
metric = "scale.list_json.per_entry_us"
max = 250
tier = "target"
note = "list(1000) - list(0) at most 250 ms, i.e. 250 us per entry"

[[budget]]
metric = "tui.first_idle.n1000.median_ms"
max = 800
tier = "target"
note = "TUI first idle at 1000 entries"

[[budget]]
metric = "rss.version.peak_kib"
max = 65536
tier = "target"
note = "CLI peak RSS at most 64 MiB"

[[budget]]
metric = "rss.list_json.n1000.peak_kib"
max = 65536
tier = "target"
note = "CLI peak RSS at most 64 MiB, even at 1000 entries"

[[budget]]
metric = "tui.rss.n1000.peak_kib"
max = 98304
tier = "target"
note = "TUI peak RSS at most 96 MiB at 1000 entries"

[[budget]]
metric = "footprint.closure_bytes"
max = 67108864
tier = "target"
profiles = [
    "full",
]
note = "full dependency closure at most 64 MiB (excludes interpreter, uv, caches, per-script deps)"

[[budget]]
metric = "imports.version.has_typer"
max = 0
tier = "target"
note = "fast-path acceptance: --version imports no typer"

[[budget]]
metric = "imports.version.has_rich"
max = 0
tier = "target"
note = "fast-path acceptance: --version imports no rich"

[[budget]]
metric = "imports.version.has_textual"
max = 0
tier = "target"
note = "fast-path acceptance: --version imports no textual"

[[budget]]
metric = "imports.version.has_tree_sitter"
max = 0
tier = "target"
note = "fast-path acceptance: --version imports no tree-sitter"

[[budget]]
metric = "syscalls.list_json.network"
max = 0
tier = "target"
profiles = [
    "full",
]
note = "warm read-only paths do zero network syscalls"
