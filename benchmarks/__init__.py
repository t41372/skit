"""skit's performance evaluation pipeline (docs/design/benchmarks.md).

Dev tooling — never shipped in the wheel, exempt from the i18n gate (which scans
src/skit only). Front door: `uv run python -m benchmarks --help`.
"""
